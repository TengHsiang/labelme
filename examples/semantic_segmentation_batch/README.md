# Semantic Segmentation to Gray Map Example

## Batch convert json to gray map and visualize map

```bash
labelme_json_to_dataset_bat --label-file label.txt --json-list json.txt '{shift_auto_shape_color: -2}'
```